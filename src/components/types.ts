export interface DefaultSettingsTypes {
  gamePaused: boolean;
  easyMode: boolean;
  freeVowels: boolean;
  volume: number;
  muted: boolean;
  music: boolean;
}

export interface KeyWordTypes {
  countryName: string;
  countryFlag: string;
}
